# Base URL of the backend API. Vite dev server also proxies /api to
# http://localhost:5000 by default (see vite.config.js), so this is
# mainly needed for production builds pointing at a deployed backend.
VITE_API_URL=/api
